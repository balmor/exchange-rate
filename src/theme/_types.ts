export enum ThemeMode {
  Dark = 'dark',
}

export type StyledTheme = {
  primary: string;
  primaryDarken: string;
  secondary: string;
  third: string;
  textPrimary: string;
  textSecondary: string;
};
